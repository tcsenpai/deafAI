OPENAI_URL=http://127.0.0.1:8080/v1
MODEL="model"
OPENAI_API_KEY="your-api-key"
DEAF_LEVEL=5
# Language: en (English), it (Italian), agnostic (Language-agnostic)
LANGUAGE=en
# Web UI port (for bun run web)
WEB_PORT=3000
# Dev server port (for bun run dev) - defaults to WEB_PORT + 1
DEV_PORT=3001
# Optional system prompt for the AI
# Keep it minimal - we want to see how the LLM handles degraded input naturally
SYSTEM_PROMPT="You are a helpful assistant. Always respond in the same language the user writes to you. If you don't understand something, ask for clarification naturally."
